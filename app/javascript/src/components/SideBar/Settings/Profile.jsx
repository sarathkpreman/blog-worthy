import React from "react";

const Profile = () => (
  <div className="profile-settings flex items-center space-x-2">
    <i className="ri-user-fill" />
    <span>Profile</span>
  </div>
);

export default Profile;
